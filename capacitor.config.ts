import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'projet-tinder-esiee',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
